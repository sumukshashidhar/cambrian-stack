# Baseline autoregressive transformer config

defaults:
  - experiment: autoregressive
  - model: transformer
  - data: tiny_stories
  - training: transformer
  - logging: default
  - output: default
  - _self_

logging:
  wandb_run: baseline-d12

output:
  dir: out/baseline-d12
